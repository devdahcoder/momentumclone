export const mapIcon = (icon) => {
    let file = `../../Assets/weatherIcon/`;

    // for (let index = 0; index <= 50; index++) {
    //     if (index === icon) {
    //         console.log(file + icon + ".png")
    //     }
    // }

    return file + icon + ".png"
}

// console.log(mapIcon(2))